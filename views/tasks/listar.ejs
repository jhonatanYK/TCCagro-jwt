<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Serviços - Gestor de Serviços</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-900 text-white">
    <%- include('../partials/notifications') %>
    
    <div class="min-h-screen flex flex-col justify-center items-center px-4 py-8">
      <div class="flex flex-col items-center mb-10">
        <img src="/trator.png" alt="Ícone" class="w-20 h-20 mb-4">
        <h2 class="text-2xl font-bold text-teal-400">Meus Serviços</h2>
      </div>

      <div class="bg-gray-800 rounded-2xl shadow-2xl p-8 w-full max-w-4xl border border-teal-600">
        <div class="flex justify-end items-center mb-6">
          <a
            href="/tasks/new"
            class="bg-teal-600 hover:bg-teal-700 text-white font-bold rounded-lg px-6 py-3 transition border-2 border-teal-400 inline-flex items-center gap-2 shadow-lg hover:shadow-xl"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            Novo Serviço
          </a>
        </div>

        <% if (tasks.length === 0) { %>
          <p class="text-gray-400 text-center py-8">Nenhum serviço cadastrado ainda.</p>
        <% } else { %>
        <div class="overflow-x-auto">
          <table class="w-full">
            <thead>
              <tr class="border-b border-gray-700">
                <th class="text-left py-3 px-4 text-teal-300">Data</th>
                <th class="text-left py-3 px-4 text-teal-300">Cliente</th>
                <th class="text-left py-3 px-4 text-teal-300">Serviço</th>
                <th class="text-left py-3 px-4 text-teal-300">Máquina</th>
                <th class="text-left py-3 px-4 text-teal-300">Horímetro</th>
                <th class="text-left py-3 px-4 text-teal-300">Horas</th>
                <th class="text-left py-3 px-4 text-teal-300">Valor Total</th>
                <th class="text-center py-3 px-4 text-teal-300">Status</th>
                <th class="text-center py-3 px-4 text-teal-300">Ações</th>
              </tr>
            </thead>
            <tbody>
              <% tasks.forEach(function(task) { %>
              <tr class="border-b border-gray-700 hover:bg-gray-700">
                <td class="py-3 px-4 text-gray-300">
                  <% if (task.service_date) { %>
                    <%= task.service_date.split('T')[0].split('-').reverse().join('/') %>
                  <% } else { %>
                    -
                  <% } %>
                </td>
                <td class="py-3 px-4 text-white"><%= task.client ? task.client.name : '-' %></td>
                <td class="py-3 px-4 text-white max-w-xs">
                  <div class="whitespace-pre-wrap break-words"><%= task.serviceName %></div>
                </td>
                <td class="py-3 px-4 text-gray-300">
                  <% if (task.machines && task.machines.length > 0) { %>
                    <% task.machines.forEach(function(machine, index) { %>
                      <div class="<%= index > 0 ? 'mt-2 pt-2 border-t border-gray-600' : '' %>">
                        <%= machine.name %>
                        <span class="text-xs text-gray-400">(<%= machine.type %>)</span>
                      </div>
                    <% }); %>
                  <% } else if (task.machine) { %>
                    <%= task.machine.name %>
                    <span class="text-xs text-gray-400">(<%= task.machine.type %>)</span>
                  <% } else { %>
                    -
                  <% } %>
                </td>
                <td class="py-3 px-4 text-gray-300">
                  <% if (task.machines && task.machines.length > 0) { %>
                    <% task.machines.forEach(function(machine, index) { %>
                      <div class="<%= index > 0 ? 'mt-2 pt-2 border-t border-gray-600' : '' %> text-xs">
                        <% const tm = machine.task_machine; %>
                        <% if (tm && tm.startTime) { %>
                          Inicial: <%= tm.startTime %>
                          <% if (tm.endTime) { %>
                            <br>Final: <%= tm.endTime %>
                          <% } else { %>
                            <br><span class="text-yellow-400">⏳ Em andamento</span>
                          <% } %>
                        <% } else { %>
                          -
                        <% } %>
                      </div>
                    <% }); %>
                  <% } else { %>
                    -
                  <% } %>
                </td>
                <td class="py-3 px-4 text-gray-300">
                  <% 
                    let totalHours = 0;
                    if (task.machines && task.machines.length > 0) {
                      task.machines.forEach(function(machine) {
                        const tm = machine.task_machine;
                        if (tm && tm.hoursWorked) {
                          totalHours += parseFloat(tm.hoursWorked);
                        }
                      });
                    }
                  %>
                  <% if (totalHours > 0) { %>
                    <%= totalHours.toFixed(1).replace('.', ',') %>h
                  <% } else { %>
                    <span class="text-gray-500">-</span>
                  <% } %>
                </td>
                <td class="py-3 px-4 font-semibold">
                  <% 
                    let totalAmount = 0;
                    if (task.machines && task.machines.length > 0) {
                      task.machines.forEach(function(machine) {
                        const tm = machine.task_machine;
                        if (tm && tm.totalAmount) {
                          totalAmount += parseFloat(tm.totalAmount);
                        }
                      });
                    }
                  %>
                  <% if (totalAmount > 0) { %>
                    <span class="text-green-400">R$ <%= totalAmount.toFixed(2).replace('.', ',') %></span>
                  <% } else { %>
                    <span class="text-gray-500">-</span>
                  <% } %>
                </td>
                <td class="py-3 px-4 text-center">
                  <% 
                    let allFinished = true;
                    if (task.machines && task.machines.length > 0) {
                      task.machines.forEach(function(machine) {
                        const tm = machine.task_machine;
                        if (!tm || !tm.endTime) {
                          allFinished = false;
                        }
                      });
                    } else {
                      allFinished = false;
                    }
                  %>
                  <div class="flex flex-col gap-2 items-center">
                    <% if (allFinished && task.machines?.length > 0) { %>
                      <span class="text-green-400 font-semibold">✓ Finalizado</span>
                      <% if (task.paid) { %>
                        <span class="bg-green-500 text-white px-3 py-1 rounded-full text-sm font-bold">💰 PAGO</span>
                      <% } else { %>
                        <span class="bg-red-500 text-white px-3 py-1 rounded-full text-sm font-bold">❌ NÃO PAGO</span>
                      <% } %>
                    <% } else { %>
                      <span class="text-yellow-400 font-semibold">⏳ Em Andamento</span>
                    <% } %>
                  </div>
                </td>
                <td class="py-3 px-4">
                  <div class="flex gap-2 justify-center flex-wrap">
                    <!-- Botão PDF (apenas quando finalizado) -->
                    <% if (allFinished && task.machines?.length > 0) { %>
                      <a
                        href="/tasks/pdf/<%= task.id %>"
                        target="_blank"
                        class="bg-blue-600 hover:bg-blue-700 text-white rounded-lg px-4 py-2 font-bold transition border-2 border-blue-500 inline-flex items-center gap-1 shadow-md hover:shadow-lg"
                        title="Gerar PDF"
                      >
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                        </svg>
                        PDF
                      </a>
                    <% } %>
                    
                    <% if (allFinished && task.machines?.length > 0) { %>
                      <!-- Botão de Pagamento (somente quando não está pago) -->
                      <% if (!task.paid) { %>
                        <form action="/tasks/mark-paid/<%= task.id %>" method="POST" style="display: inline;">
                          <button
                            type="submit"
                            class="bg-green-600 hover:bg-green-700 text-white rounded-lg px-4 py-2 font-bold transition border-2 border-green-500 inline-flex items-center gap-1 shadow-md hover:shadow-lg"
                            title="Marcar como pago"
                          >
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            Marcar como Pago
                          </button>
                        </form>
                      <% } %>
                    <% } else { %>
                      <!-- Botão Finalizar (quando ainda não foi finalizado) -->
                      <a
                        href="/tasks/edit/<%= task.id %>"
                        class="bg-green-600 hover:bg-green-700 text-white rounded-lg px-4 py-2 font-bold transition border-2 border-green-500 inline-flex items-center gap-1 shadow-md hover:shadow-lg"
                      >
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        Finalizar
                      </a>
                    <% } %>
                    <form id="delete-form-<%= task.id %>" action="/tasks/delete/<%= task.id %>" method="POST" style="display: inline;">
                      <button
                        type="button"
                        class="bg-red-600 hover:bg-red-700 text-white rounded-lg px-4 py-2 font-bold transition border-2 border-red-500 inline-flex items-center gap-1 shadow-md hover:shadow-lg"
                        onclick="handleDelete(<%= task.id %>, 'serviço')"
                      >
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                        </svg>
                        Excluir
                      </button>
                    </form>
                  </div>
                </td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
        <% } %>

        <!-- Botão Voltar ao Painel -->
        <div class="mt-8">
          <a
            href="/dashboard"
            class="block text-center bg-gray-700 hover:bg-gray-600 text-white font-bold py-3 px-6 rounded-lg border-2 border-gray-600 w-full shadow-md hover:shadow-lg transition duration-200"
          >
            Voltar ao Painel
          </a>
        </div>
      </div>
    </div>

    <%- include('../partials/auth-check') %>

    <script>
      // Função para lidar com exclusão
      async function handleDelete(id, type) {
        const confirmed = await customConfirm(`Tem certeza que deseja excluir este ${type}?`);
        if (confirmed) {
          document.getElementById(`delete-form-${id}`).submit();
        }
      }
    </script>
  </body>
</html>
